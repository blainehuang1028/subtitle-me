# Neutral demo

This original 20-cue fixture demonstrates the complete workflow without using any real product, franchise, creator, or external subtitle data.

The test creates a project with an empty glossary, extracts three fictional terms, applies the supplied decisions, writes the supplied Chinese semantic translation, generates readable subtitles and bilingual ASS, completes a second AI review, and runs deterministic QA.
